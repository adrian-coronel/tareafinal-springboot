spring:
  datasource:
    url: jdbc:mysql://localhost:3306/db_tareafinal
    username: root
    password: miryam2003
    driver-class-name: com.mysql.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        jdbc:
          batch_size: 20
        order_updates: true
    database: mysql
    database-platform: org.hibernate.dialect.MySQLDialect